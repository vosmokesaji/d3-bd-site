# BD 内容与界面重构路线图

更新日期：2026-08-16

本文档把 BD 多场景配置、巅峰加点、可替换项、随从纸娃娃、装备框和全站字号六项工作拆成可独立验收的阶段。原则是先建立能表达真实差异的数据结构，再逐个 BD 校对；不通过工厂默认值批量制造看似完整的配置。

AI 接手续作时先阅读 [BD 逐套校对 AI 交接手册](./ai-handoff-bd-review.md)，其中记录当前游标、单套实施流程、验收、文档更新与提交规范。

## 进度总览

| 阶段 | 状态 | 已完成 | 剩余工作 |
| --- | --- | --- | --- |
| 阶段 0：基线与文档 | 部分完成 | 风险结论、51 套清单、单套校验器 | 增加覆盖 51 套 BD 的机器可读审计报告脚本 |
| 阶段 1：数据契约与校验器 | 已完成 | 新契约、patch 解析、diff、校验器、塔格奥迁移 | 无；后续只做回归维护 |
| 阶段 2：通用视觉底座 | 已完成 | 统一装备框、五处迁移、物品详情收敛、字号 token、桌面/手机抽查与回归测试 | 无；固定尺寸 BD 装备盘在窄屏使用自身横向滚动，不再撑宽页面 |
| 阶段 3：随从纸娃娃 | 已完成 | 共享组件、显式 14 槽、空槽、技能区、桌面/平板/手机布局 | 无；后续只随数据和视觉基线做回归维护 |
| 阶段 4：BD 逐套校对 | 部分完成 | 5 套完整、0 套局部显式配置 | 46 套逐项校对（合计剩余 46 套） |
| 阶段 5：总体验收 | 未开始 | 已有构建与关键路由回归测试 | 51 套审计、桌面/手机视觉回归和最终验收 |

## 1. 当前结论

### 1.1 BD 配置并未真正完成

项目有 51 套 BD，但当前数据完整度不能用“有四份 `variantProfiles`”来判断：

- 塔格奥死亡新星已完成私有解析器样板；荒原旋风痛割已完成通用 `configurationBase + scenarios` 运行时样板。
- 跑马天拳已按金币链与掉金币边界完成四场景校对，装备、技能、宝石、威能与循环由场景配置驱动；蓝门并入 T16 速刷场景，不扩展 `BuildContent`。
- 恐惧冰吞已按武器组合（杨弓速刷 / 盾枪-维拉冲层）与扫射符文差异完成四场景校对，蓝门并入速刷场景。
- 上帝僧已把两套配装迁入四场景（T16/蓝门用尹娜混搭水幻身清屏、地精/外观用千飓六件纯位移），`loadouts` 与 `powerSets` 已移除，mode 切换即配装切换。
- 其余 46 套主要依赖工厂生成的说明文字和通用优先级，页面已经停止自动替换未经校对的装备，但也因此会出现“切换后实际配置不变”。
- `variantCompleteness: "complete"` 目前只证明存在某个 `powerSets` 或 `loadouts` 字段，不能证明所有适用场景都已校对。

当前正确状态是：5 套已按新契约完整评审、0 套部分实装、46 套待逐项校对，合计仍需完成 46 套。

### 1.2 当前模型表达能力不足

现有 `BuildVariantProfile` 只能表达通用词缀策略、移动鞋开关、一个传奇宝石替换和最后一个魔方威能替换。它不能可靠表达：

- 某个装备槽替换成什么，以及替换条件和代价。
- 技能、符文、被动、卡奈魔方、传奇宝石和普通宝石的成组变化。
- 大秘境冲层、大秘境速刷、T16 小秘境、悬赏、蓝门、梦魇回响和外观收集等真实用途。
- 低巅峰成型期、800 点前后、2000+ 高巅峰以及装备词缀洗掉主属性后的不同加点策略。
- 某项必须固定、条件可换或自由选择，以及替换后会破坏哪些联动。
- “此场景确实无需变化”的可审计原因。

### 1.3 装备视觉存在多个实现

当前至少有两套装备图标实现：BD 纸娃娃直接渲染图片和槽位背景，物品库使用 `BlizzardItemIcon`。BD 装备详情又维护了独立的 `detail-item-icon`。这会导致尺寸、品质底纹、边框、裁切和镶孔位置继续分叉。

目标是收敛为一个底层 `DiabloItemFrame`，所有页面只传物品数据、形状、尺寸和交互状态。

### 1.4 字号基线过小

`app/globals.css` 中仍有大量 6–9px 的正文、说明和列表文字。后续不应逐个选择器临时放大，而应先建立全站文字 token，再按语义替换。

## 2. 实施原则

1. 每个 BD 单独确认适用内容，不强迫所有 BD 都拥有同一组场景。
2. 每个适用场景都保存真实配置；不适用时明确记录原因，不伪造差异。
3. 页面差异摘要由配置 diff 计算，不由人工文案假装发生了变化。
4. 工厂只补结构性默认值，不自动替换装备、宝石、技能或魔方威能。
5. 每项替换必须有触发条件、收益、代价和互斥关系。
6. 巅峰建议必须区分 800 点前固定分类加点和 800 点后主属性/体能分配。
7. 每套 BD 保存资料版本、平台、来源和校对日期；来源冲突时保留判断说明。
8. 基础组件完成后再批量填内容；每套 BD 仍独立提交、独立测试、独立验收。

## 3. 优先级

| 优先级 | 工作 | 原因 | 完成标志 |
| --- | --- | --- | --- |
| P0 | 重建 BD 场景与可替换项数据契约 | 现有模型无法承载真实配置，继续填数据会反复返工 | 数据可表达完整配装、替换条件、巅峰计划和来源 |
| P0 | 增加 BD 数据校验与真实 diff | 防止“文案变化、配置不变”再次被标记为完成 | 构建时能列出缺失场景、空差异和失效引用 |
| P0 | 逐个校对 51 套 BD | 这是用户看到的核心内容正确性 | 每套 BD 有独立校对记录和验收测试 |
| P1 | 统一装备框组件 | 四个页面正在使用不同展示规则，越晚收敛返工越大 | BD、随从、物品列表和详情共用底层组件 |
| P1 | 重做随从纸娃娃 | 当前三栏结构有雏形，但装备槽、人物和技能区未达到参考图比例 | 桌面三栏与移动端单栏均通过视觉验收 |
| P1 | 建立全站字号/行高 token | 当前 6–9px 描述影响阅读，也会干扰后续布局验收 | 说明文字不再散落硬编码极小字号 |
| P2 | 拆分 BD 详情和样式文件 | 降低 51 套逐项修改时的回归风险 | 场景、巅峰、替换项、随从和装备框各自有组件边界 |

## 4. 目标数据模型

下面是目标能力，不要求一次提交全部实现；类型应按阶段逐步落地。

### 4.1 场景不是固定四格

用真实内容类型替代模糊的 `push/speed` 二选一：

```ts
type BuildContent =
  | "greater-rift-push"
  | "greater-rift-speed"
  | "nephalem-rift"
  | "bounty"
  | "echoing-nightmare"
  | "cosmetic-farm";

type ParagonBand = "pre-800" | "low" | "high";

type BuildScenario = {
  id: string;
  content: BuildContent;
  paragonBand: ParagonBand;
  applicability: "supported" | "not-recommended" | "not-applicable";
  reason: string;
  configuration?: BuildConfiguration;
  sourceRefs: string[];
  reviewedAt?: string;
};
```

页面仍可提供易懂的“用途”和“巅峰”控件，但选项必须来自该 BD 的 `scenarios`，不能先生成笛卡尔积再假定都有意义。

### 4.2 配置必须是可比较的完整对象

```ts
type BuildConfiguration = {
  gear: EquippedItem[];
  skills: EquippedSkill[];
  passives: string[];
  powers: EquippedPower[];
  legendaryGems: EquippedGem[];
  normalGems: GemRule[];
  follower: FollowerConfiguration;
  statPriorities: StatPriority[];
  rotation: RotationStep[];
};
```

实现可以采用“基准配置 + 显式 patch”减少重复，但解析后必须得到完整对象。校验器比较解析结果，确认页面所写的每条变化真的发生。

### 4.3 固定项与可替换项

装备、技能、符文、被动、威能和宝石统一使用选择策略：

```ts
type ChoicePolicy = {
  status: "locked" | "conditional" | "flexible";
  reason: string;
  alternatives: Array<{
    id: string;
    when: string;
    gain: string;
    cost: string;
    incompatibleWith?: string[];
    scenarios?: string[];
  }>;
};
```

界面文案约定：

- `必须固定`：构筑发动机、套装件数、关键乘区或生存闭环，替换会让 BD 失效。
- `条件替换`：在特定层数、巅峰、坚韧、冷却、组怪效率或玩法中替换。
- `自由槽位`：不破坏核心联动，可按词缀质量或个人操作选择。

所有替换都必须回答“什么时候换、换成什么、得到什么、失去什么”。

### 4.4 巅峰加点指导

每个 BD 增加 `paragonGuide`，至少覆盖：

- 800 点前：核心、进攻、防御、辅助四类的逐项顺序。
- 移速规则：角色面板 25% 上限中，装备移速与巅峰移速如何配合。
- 资源上限：是否增加伤害、循环或防御；无收益时明确跳过。
- 800 点后：主属性与体能的分配原则，不使用一条固定比例套全部 BD。
- 低巅峰保命阈值：什么情况下先补体能，达到什么实战条件后转主属性。
- 高巅峰词缀联动：装备洗掉主属性后，巅峰如何补回；范围伤、冷却、攻速断点如何影响选择。
- 主机说明：Nintendo Switch 操作、锁定和拾取差异会影响的加点或配置。

```ts
type ParagonGuide = {
  pre800: Record<"core" | "offense" | "defense" | "utility", ParagonPriority[]>;
  post800: Array<{
    condition: string;
    mainStat: string;
    vitality: string;
    rationale: string;
  }>;
  checkpoints: Array<{
    label: string;
    target: string;
    adjustment: string;
  }>;
};
```

## 5. 页面设计计划

### 5.1 BD 场景切换

切换后页面顶部展示自动计算的“本配置发生了什么”：

- 装备：新增、移除、替换。
- 技能/符文/被动：替换项及原因。
- 魔方和宝石：替换项及适用内容。
- 词缀和巅峰：优先级或分配策略变化。
- 手法：新增、删除或调整的步骤。

如果两个场景配置一致，必须显示人工校对过的 `reason`，并且不能把该状态计为“有差异”。

### 5.2 固定与替换说明

纸娃娃和技能区使用简短状态标记；点击后在详情区展示完整替换条件。不要把全部说明堆在装备盘上。

详情区顺序：

1. 当前物品在 BD 中的职责。
2. 固定程度及原因。
3. 可替换方案、触发条件、收益与代价。
4. 当前场景为什么选它。
5. 词缀、镶嵌、获取和避坑。

### 5.3 统一装备框

新增底层 `DiabloItemFrame`，由它统一处理：

- 普通/制作、传奇、套装的品质底纹和边框。
- 方形、普通竖框、胸甲大竖框等官方比例。
- 原图居中裁切，不拉伸成正方形。
- 镶孔宝石、悬停、选中、关联高亮和禁用状态。
- `sm`、`md`、`lg` 三档尺寸；比例由物品分类决定。

复用位置：

- BD 角色纸娃娃装备槽。
- BD 装备详情左侧。
- 随从纸娃娃装备槽。
- 物品 Tab 列表左侧。
- 物品详情左侧。

物品详情移除大幅氛围背景图，左侧只保留装备框；右侧继续显示结构化物品信息。

### 5.4 随从纸娃娃

视觉基线采用用户截图和[暴雪角色页](https://us.diablo3.blizzard.com/zh-tw/profile/DyS-1311/hero/159448381)的布局关系，不复制网页代码：

- 桌面端三名随从并排，每栏独立标题、人物半身/全身和装备盘。
- 人物在上半区偏右，装备槽围绕人物分布；武器和副手位于底部两侧。
- 未装备槽保留暗色轮廓，已装备槽使用统一装备框。
- 技能位于装备盘下方，保持 2×2 排列；说明和推荐原因放在技能区之后。
- 推荐随从只通过标题、边框和状态文字强调，不改变槽位几何。
- 移动端改为单栏逐名展示，不把三栏压缩到不可读尺寸。

第一阶段继续使用项目已有的本地随从人物素材；若素材比例不足，再单独补资源，不把人物照片裁成装备背景。

### 5.5 全站字号与行高

先在 `:root` 建立语义 token，再替换散落字号：

```css
--text-caption-size: 12px;
--text-caption-line: 1.5;
--text-meta-size: 13px;
--text-meta-line: 1.55;
--text-small-size: 14px;
--text-small-line: 1.65;
--text-body-size: 16px;
--text-body-line: 1.75;
--text-heading-sm-size: 18px;
--text-heading-sm-line: 1.4;
```

执行规则：

- 正文、装备用途、替换条件和攻略说明不低于 14px。
- 12px 仅用于槽位标签、次要元信息和装饰性题注。
- 6–9px 不再用于需要阅读的内容。
- 字号不按视口宽度连续缩放；窄屏通过换行、布局重排和容器宽度解决。
- 每一阶段都检查按钮、标签、装备框和三栏随从区是否因字号增加发生遮挡。

## 6. 分阶段实施与提交

### 阶段 0：基线与文档

- [x] 记录真实完整度和当前风险。
- [x] 建立 51 套 BD 校对清单。
- [ ] 增加机器可读的审计报告脚本，输出每套 BD 的场景、diff、巅峰和替换项完整度。**剩余：**当前校验器只在已迁移 BD 上执行，还没有生成全站 51 套汇总报告。

建议提交：`docs: 制定 BD 内容与界面重构路线`

### 阶段 1：数据契约与校验器

- [x] 引入场景、完整配置、选择策略、巅峰指导和来源记录类型。
- [x] 支持基准配置加显式 patch，并解析为完整配置。
- [x] 校验重复槽位、失效 ID、空场景、伪差异、无条件替换和缺失来源。
- [x] 完整度改为按能力计算，例如 `draft`、`scenario-reviewed`、`fully-reviewed`。
- [x] 迁移塔格奥死亡新星；四个场景由完整配置驱动，并展示真实 diff、巅峰和替换策略。

完成记录：`tragoul-nova` 已于 2026-08-14 按赛季 39 / 2.7.8 资料校对。低巅峰冲层使用守护者过渡，高巅峰冲层使用奥吉德；低巅峰 T16 保留守护者并切换聚怪/移速/金币宝石，高巅峰 T16 使用塔格奥肩、沃兹克、金织带和贪婪之戒。构建时会执行 `validateReviewedBuildGuide`，页面差异由 `diffBuildConfigurations` 生成。

### 阶段 2：通用视觉底座

- [x] 实现 `DiabloItemFrame` 并迁移 `BlizzardItemIcon`。
- [x] 迁移 BD 纸娃娃、BD 详情、物品列表、物品详情和随从装备，两处随从入口共用同一路径。
- [x] 物品详情删除大背景图，左侧只保留统一装备框。
- [x] 建立全站字号/行高 token，首批覆盖 BD 场景说明、装备详情、物品页和随从说明。
- [x] 为统一组件、五处消费者、品质底纹、固定比例和字号 token 增加回归测试。
- [x] 修复统一装备框迁移回归：纸娃娃异形装备恢复越界展示，右侧详情图改为填满式缩放并校正宝石位置。
- [x] 按孔数校正装备详情镶孔：单孔固定右下角，双孔和三孔沿右边缘垂直居中，不再遮挡效果说明。
- [x] 隔离装备详情图片通用规则，固定镶孔圆框为 32px、宝石芯为 20px 并保持同心。
- [x] 完成小屏响应式验收：390px 页面不再产生横向溢出，固定尺寸 BD 装备盘改为在自身容器内横向滚动，装备详情回到视口宽度。

完成记录：新增 `components/items/DiabloItemFrame.tsx` 作为唯一装备图渲染底座，统一普通/制作、传奇、套装品质底纹，支持方形、标准竖框、胸甲大竖框和槽位填充比例。BD 与随从仍保留各自的槽位几何和交互，但不再自行绘制装备背景、品质边框、图片裁切或镶孔。

### 阶段 3：随从纸娃娃

- [x] 抽出 `FollowerPaperdoll`、`FollowerCard`、`FollowerShowcase` 和显式槽位映射。
- [x] 按暴雪比例完成三种随从的人物、装备槽与底部武器/副手布局。
- [x] 接入统一装备框、暗色空槽、2×2 技能区和推荐状态。
- [x] 验证桌面 1600px 三栏、平板 900px 两栏和手机 390px 单栏。

完成记录：随从数据迁入 `app/data/followers.ts`，每名随从固定解析为 14 个槽位，不再根据数组顺序猜测戒指和专属饰品位置；魔女与盗贼明确展示未装备的副手暗槽。两个 BD 详情入口共用同一展示组件。1600px、900px、390px 视口下分别得到三栏、两栏、单栏，页面宽度等于视口宽度，随从图片加载失败数为 0。**剩余：**阶段 3 无功能剩余；后续新增随从配装时必须继续提供显式 `position` 并通过 14 槽回归测试。

### 阶段 4：BD 逐套校对

- [x] `tragoul-nova` 塔格奥死亡新星：四场景、巅峰、固定/替换策略与回归测试已完成。
- [x] `wastes-rend` 荒原旋风痛割：四场景、巅峰、固定/替换策略与运行时切换已完成。
- [x] `pony-fist-farm` 跑马天拳：四场景、巅峰、固定/替换策略与回归测试已完成。
- [x] `god-hungering` 恐惧冰吞：四场景、巅峰、固定/替换策略与回归测试已完成。
- [x] `god-monk` 上帝僧：两套配装迁入四场景、巅峰、固定/替换策略与回归测试已完成。
- [ ] 其余 46 套 BD：按下方清单顺序逐套校对，不复用未经确认的差异。

上帝僧完成记录：以第39赛季、2.7.8资料为基准，T16/蓝门用尹娜五件＋千飓三件由水幻身清屏，地精/外观用千飓六件纯位移；皇家华戒是两套共同连接器。移除 `loadouts` 与 `powerSets`，mode 切换即配装切换；武器萃取按寅剑/怒涌区分，第4槽按粗糙靴/梅斧区分。通用详情页由 `configurationBase + scenarios` 驱动装备、技能、威能、随从、词缀与循环，`diffBuildConfigurations` 输出装备、技能、威能、随从、词缀与循环六类真实差异。**剩余：**阶段4还有46套；下一套为 `mundunugu-barrage`。

每套 BD 使用同一流程，但不能共享未经确认的内容：

1. 确认赛季、平台、补丁和来源。
2. 确认适用内容以及不推荐内容。
3. 建立每个适用场景的完整配置。
4. 填写 800 点前、低巅峰和高巅峰加点。
5. 标记装备、技能、符文、被动、威能和宝石的固定程度。
6. 填写每个替换项的条件、收益、代价和互斥关系。
7. 检查自动 diff 与页面说明一致。
8. 添加该 BD 的数据测试和关键页面测试。
9. 单独提交，提交正文写明来源、差异和验证结果。

建议提交格式：`feat(bd): 校对荒原旋风痛割多场景配置`

### 阶段 5：总体验收

- [ ] 审计报告中 51 套 BD 均达到 `fully-reviewed`，或有明确的不适用原因。
- [ ] 任意场景切换都能看到真实 diff 或已校对的无差异原因。
- [ ] 任意装备/技能/威能都能回答是否固定以及如何替换。
- [ ] 所有 BD 都有可执行的巅峰加点指导。
- [ ] 五处装备展示共用统一装备框。
- [x] 随从布局和字号在桌面、平板与移动端完成视觉回归。
- [ ] 完整测试、构建和逐页抽查通过后再更新“已完成”状态。

## 7. 51 套 BD 校对清单

状态说明：`样板` 表示需要迁移并防回退；`部分` 表示已有局部显式数据但未满足全部要求；`待校对` 表示当前主要是共享配置和说明。

| 顺序 | 职业 | BD | ID | 当前状态 | 本轮重点 |
| ---: | --- | --- | --- | --- | --- |
| 1 | 死灵法师 | 塔格奥死亡新星 | `tragoul-nova` | 已校对 | 新契约基线；保留回归测试 |
| 2 | 野蛮人 | 荒原旋风痛割 | `wastes-rend` | 已校对 | 四场景真实配置与通用运行时基准已完成 |
| 3 | 圣教军 | 跑马天拳 · 全能速刷 | `pony-fist-farm` | 已校对 | 蓝门并入T16速刷、大秘境金币链边界与寅剑适用边界 |
| 4 | 猎魔人 | 恐惧冰吞 | `god-hungering` | 已校对 | 杨弓速刷与盾枪/维拉冲层的武器与符文差异 |
| 5 | 武僧 | 上帝僧 · 无限疾风 | `god-monk` | 已校对 | 两套配装迁入四场景、loadouts 移除 |
| 6 | 巫医 | 蒙嘟噜魂弹 | `mundunugu-barrage` | 待校对 | 延迟结算与冲层专用配置 |
| 7 | 魔法师 | 塔拉夏陨石 | `tal-meteor` | 待校对 | 四系触发和资源/坚韧变化 |
| 8 | 野蛮人 | 蕾蔻巨石 | `raekor-boulder` | 待校对 | 冲层与速刷技能/武器变化 |
| 9 | 野蛮人 | 不朽先祖锤 | `ik-hota` | 待校对 | 成型期与高巅峰词缀变化 |
| 10 | 野蛮人 | 梦遗先祖锤 | `lod-hota` | 待校对 | 远古门槛和不推荐速刷条件 |
| 11 | 野蛮人 | 大地跃击 | `earth-leapquake` | 待校对 | 冷却、控制和机动替换 |
| 12 | 野蛮人 | 九十蛮狂乱 | `h90-frenzy` | 待校对 | 单体冲层与低层赶路差异 |
| 13 | 野蛮人 | 不朽冲锋 | `ik-charge` | 待校对 | 速刷专用场景边界 |
| 14 | 圣教军 | 勇气天拳 | `valor-fist` | 待校对 | 与跑马天拳区分定位 |
| 15 | 圣教军 | 勇气天堂之怒 | `valor-fury` | 待校对 | 双技能叠层与爆发窗 |
| 16 | 圣教军 | 阿克汉天谴 | `akkhan-condemn` | 待校对 | 冷却阈值和速刷替换 |
| 17 | 圣教军 | 阿克汉圣军 | `akkhan-phalanx` | 待校对 | 宠物攻速和坚韧配置 |
| 18 | 圣教军 | 唤魔荆棘 | `invoker-thorns` | 待校对 | 荆棘词缀与首领向配置 |
| 19 | 圣教军 | 罗兰横扫 | `roland-sweep` | 待校对 | 资源、攻速和控制变化 |
| 20 | 圣教军 | 圣光锤丁 | `seeker-hammer` | 待校对 | 落剑覆盖和机动方案 |
| 21 | 圣教军 | 梦遗轰击 | `lod-bombardment` | 待校对 | 荆棘、冷却和远古门槛 |
| 22 | 猎魔人 | 掠夺集束塔 | `marauder-sentry` | 待校对 | 箭塔数量、资源和冲层手法 |
| 23 | 猎魔人 | 不洁多重射击 | `ue-multishot` | 待校对 | T16、悬赏、低层大秘境 |
| 24 | 猎魔人 | 娜塔亚尖刺陷阱 | `natalya-trap` | 待校对 | 引爆技能与高低巅峰生存 |
| 25 | 猎魔人 | 暗影三刀 | `shadow-impale` | 待校对 | 精英点杀和资源循环 |
| 26 | 猎魔人 | 梦遗连射 | `lod-rapid-fire` | 待校对 | 站桩冲层和不推荐场景 |
| 27 | 武僧 | 尹娜幻身 | `inna-ally` | 待校对 | 水/火幻身和用途差异 |
| 28 | 武僧 | 正义风雷冲 | `poj-tempest` | 待校对 | 引导、爆发和速刷变化 |
| 29 | 武僧 | 猴王风雷冲 | `sunwuko-tempest` | 待校对 | 手动冰爆与高层配置 |
| 30 | 武僧 | 猴王敲钟 | `sunwuko-wol` | 待校对 | T16 与低层大秘境差异 |
| 31 | 武僧 | 梦遗敲钟 | `lod-wol` | 待校对 | 远古门槛和宠物乘区 |
| 32 | 武僧 | 乌莲娜爆裂掌 | `uliana-palm` | 待校对 | 传播、引爆和聚怪配置 |
| 33 | 武僧 | 千飓疾风击 | `raiment-dash` | 待校对 | 伤害型与赶路型边界 |
| 34 | 死灵法师 | 梦遗死亡新星 | `lod-nova` | 待校对 | 远古门槛和塔格奥转型 |
| 35 | 死灵法师 | 伊纳瑞斯死亡新星 | `inarius-nova` | 待校对 | 骨甲范围与贴身生存 |
| 36 | 死灵法师 | 拉斯玛亡者大军 | `rathma-aotd` | 待校对 | 冷却循环和宠物配置 |
| 37 | 死灵法师 | 狂欢节骨矛 | `masquerade-spear` | 待校对 | 直线输出和资源变化 |
| 38 | 死灵法师 | 瘟疫尸枪 | `pestilence-lance` | 待校对 | 亡者领域窗口和冷却阈值 |
| 39 | 死灵法师 | 梦遗尸爆 | `lod-corpse-explosion` | 待校对 | 第一具尸体与领域内外配置 |
| 40 | 巫医 | 亚拉基尔蜘蛛 | `arachyr-spiders` | 待校对 | 蛛网覆盖和宠物攻速 |
| 41 | 巫医 | 魔牙愤怒鸡 | `arachyr-chicken` | 待校对 | T16 与悬赏路线 |
| 42 | 巫医 | 祖尼玛毒镖 | `zuni-darts` | 待校对 | 鬼娃同步和攻速断点 |
| 43 | 巫医 | 玉魂收割 | `jade-harvest` | 待校对 | 持续伤害结算和进场生存 |
| 44 | 巫医 | 魔牙巨尸 | `helltooth-garg` | 待校对 | 宠物配置和死亡之壁覆盖 |
| 45 | 巫医 | 梦遗魂弹 | `lod-barrage` | 待校对 | 延迟结算和远古门槛 |
| 46 | 魔法师 | 梦遗陨石 | `lod-meteor` | 待校对 | 资源、远古门槛和元素版本 |
| 47 | 魔法师 | 火鸟爆炸冲击 | `firebird-eb` | 待校对 | 点燃触发和主机操作差异 |
| 48 | 魔法师 | 德尔西尼旋风 | `delsere-twister` | 待校对 | 地形、气旋和控场配置 |
| 49 | 魔法师 | 维尔御法者 | `vyr-archon` | 待校对 | 变身内外和冷却阈值 |
| 50 | 魔法师 | 提丰多头蛇 | `typhon-hydra` | 待校对 | 蛇头数量、护盾和稳健配置 |
| 51 | 魔法师 | 梦遗冰封球 | `lod-orb` | 待校对 | 爆点距离和散件门槛 |

## 8. 单套 BD 的完成定义

一套 BD 只有同时满足以下条件才能标记 `fully-reviewed`：

- 适用场景和不适用场景均已说明。
- 每个适用场景能解析出完整且合法的配置。
- 所有场景之间的差异由程序计算，文案与 diff 一致。
- 800 点前、低巅峰、高巅峰加点完整。
- 所有装备、技能、符文、被动、威能和宝石都有固定程度。
- 所有条件替换都有触发条件、收益、代价和互斥关系。
- 因果链和输出手法引用当前场景实际存在的项目。
- 来源、赛季、平台和校对日期完整。
- 数据校验、路由测试和关键交互测试通过。
- 人工检查至少覆盖桌面和手机两个视口。

在这些条件完成前，页面可以展示“已校对部分”，但不能再用一个宽松字段把整套 BD 标记为完整。
