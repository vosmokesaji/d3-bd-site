# i18n 验证记录

验证日期：2026-09-10。

## 自动检查

- `npm run build`：通过。Vinext 完成客户端、RSC、SSR 和 Worker 构建；仅保留大于 500 kB 的分包提示及动态路由分类提示。
- `npm run typecheck`：通过。
- `node --test tests/*.test.mjs`：63/63 通过。
- 三语页面渲染测试覆盖首页、BD 总览、物品库、开荒、剧情以及全部 51 个 BD 详情；同时检查正文和 `title`、`alt`、`aria-label`，未发现原始翻译键、`null`、`译文待补齐`、客户端格式标记或未处理占位符。
- 导出测试覆盖全部 BD 的三语表格、PNG/ZIP 生成、取消和失败隔离；物品、技能、符文、套装、随从、宝石和纸娃娃属性均通过身份映射检查。
- 英文目录未发现残留汉字；机器翻译失败清单为 0。
- ESLint 对应用、组件、Worker、脚本和测试共报告 9 个错误，数量和规则与改造前基线一致：`app/page.tsx` 的 4 个原生站内链接和 4 个 effect 内同步更新状态，以及 `components/settings/SiteSettings.tsx` 的 1 个 effect 内同步更新状态。本次修改没有新增 lint 错误。

## 浏览器检查

在本地物品详情和 BD 详情中实际切换过 `zhCN` 与 `enUS`，确认设置持久化、页面标题、装备、技能、符文和属性随语言同步变化；检查过 `Intelligence`、`Critical Hit Chance`、`Dislocation`、`Guardian's Aversion` 等易混术语。中文数量单位和英文相邻术语随后增加了自动测试，确认 `80万` 保持为 `800,000`、`25码` 保持为 `25 yards`，受保护的游戏术语不会嵌套替换。

最后一次生产构建后再次启动了 `localhost:3001`，但浏览器自动审批在时限内没有完成，因此未能进行第二轮可视化复查。完整三语服务端渲染、类型检查、构建和 63 项测试均已在最终源码上通过。

## 数据边界

- 客户端术语 3,579 条；物品身份映射 2,353 条；技能及符文映射 294 组；完整描述和属性匹配 2,581 条。
- 应用目录 10,542 条，和 2,581 条客户端完整文本合计覆盖原站收集到的 13,123 条展示文案。
- 2 条旧随从技能中文客户端原文缺失、5 组旧攻略符文不能唯一确认、80 个通用别名和 71 个完整文本候选仍保持未自动认领。具体证据和候选见同目录的待核对文件。
- PC 客户端版本分别为国服 2.8.1.101167、国际服 2.8.0.99920；Switch 客户端文本与平台差异尚未核验。
